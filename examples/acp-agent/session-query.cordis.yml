# Explicit session-query tool opt-in for the dedicated spill scenario. The
# nested filesystem overlay supplies private spill storage and its byte limit.
- id: base
  name: '@williamcodebox/cordis-plugin-include'
  config:
    path: ./fs.cordis.yml
    patches:
      - insert:
          - id: tool-session-query
            name: '@williamcodebox/omd-tool-session-query'
          - id: timeout-policy
            name: '@williamcodebox/omd-tool-call-timeout-policy'
